version: '3'

services:
  pg:
    image: postgres:9.6
    environment:
      POSTGRES_DB: saas
      POSTGRES_PASSWORD: password
    ports:
      - "5432:5432"
  api:
    build: .
    volumes:
      - .:/code
    ports:
      - "5000:5000"
    environment:
      SINGLE_DATABASE_API_HOST: sdb
      ALLOWED_HOSTS: api
    depends_on:
      - pg
      - sdb
      - web
  web:
    image: saas-ui
    ports:
      - "8000:8000"
  sdb:
    image: saas-sdb
    depends_on:
      - cassandra
      - es
  es:
    image: elasticsearch:6.4.2
  cassandra:
    image: cassandra
    expose:
    - "9042"